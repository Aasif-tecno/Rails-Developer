<nav class="navbar navbar-custom navbar-expand-lg navbar-dark navbar-fixed-top" id="navbar_top">
  <div class="container">
  <%= link_to "RailsDeveloper", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item <%= current_page?(root_path) ? "active" : ""  %>">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      <% if !user_signed_in? %>
        <% if current_page?(new_user_session_path) %>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      <% end %>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <span class="avatar avatar-sm rounded-circle">
          <%  if current_user&.profile&.avatar&.attached? %>
            <%= image_tag current_user.profile.avatar, class: "rounded-circle"%>
          <% else %>
            <%= image_tag "user-img", class: "rounded-circle"%>
          <% end %>
          </span>
        </li>
        <li class="nav-item <%= current_page?(jobs_path) ? "active" : ""  %>">
            <%= link_to current_user.name, profiles_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link btn btn-danger btn-sm" %>
        </li>
      <% end %>
    </ul>
  </div>

  </div>

</nav> 
    <%  if current_page?(root_path) %>
    <div class="header pb-8 pt-5 pt-lg-8 d-flex align-items-center" style="min-height: 200px; ">
      <!-- Mask -->
      <span class="mask bg-gradient-default"></span>
      <!-- Header container-->
      <div class="container-fluid align-items-center">
        <div class="row">
          <div class="col">
            <h1 class="display-2 text-white">Find Ruby On Rails Developer</h1>
            <p class="text-white mt-0">The internet's number one source for Ruby On Rails Developer </p>
              <%= link_to "Find a developer", developers_path, class: "btn btn-primary"%>
              <%= link_to "Find a job", root_path(anchor: "card-profile"), class: "btn btn-outline-info" %>
            <p class="text-white mt-2"> or Find a Job of your wish</p>
          </div>
        </div>
      </div> 
    </div> 
    <% end %>